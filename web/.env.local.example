# ============================================================
# Taskal AI - 環境変数テンプレート
# このファイルを .env.local にコピーして値を設定してください
# cp .env.local.example .env.local
# ============================================================

# ── Supabase ────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# ── Stripe API Keys ─────────────────────────────
# Stripeダッシュボード > 開発者 > APIキー から取得
# テスト環境: pk_test_ / sk_test_ で始まるキーを使用
# 本番環境: pk_live_ / sk_live_ で始まるキーを使用
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxx
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxx

# ── Stripe Webhook Secret ───────────────────────
# ローカル: stripe listen --forward-to localhost:3000/api/stripe/webhook で取得
# 本番: Stripeダッシュボード > 開発者 > Webhook > エンドポイント > 署名シークレット
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx

# ── Stripe Price IDs ────────────────────────────
# Stripeダッシュボード > 商品カタログ > 各商品の価格ID (price_ で始まる)
STRIPE_PRICE_LIGHT_ID=price_xxxxxxxxxxxx
STRIPE_PRICE_STANDARD_ID=price_xxxxxxxxxxxx
STRIPE_PRICE_UNLIMITED_ID=price_xxxxxxxxxxxx

# ── App URL ─────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── Gemini AI ───────────────────────────────────
GEMINI_API_KEY=
MODEL_NAME=gemini-3-pro-preview
# OCR_MODEL_NAME=              # 省略時はデフォルト
# OCR_FALLBACK_MODEL_NAME=     # 省略時はデフォルト
# RATE_LIMIT_FALLBACK_MODEL=   # 省略時: gemini-2.5-pro-preview-05-06
